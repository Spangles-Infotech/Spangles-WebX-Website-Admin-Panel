// src/components/Testimonials.js
import React from "react";
import { Container, Carousel } from "react-bootstrap";

export default function Testimonials() {
  const testimonials = [
    {
      name: "John Doe",
      feedback:
        "This team delivered exceptional results! The website looks amazing and works flawlessly.",
    },
    {
      name: "Sarah Smith",
      feedback:
        "Highly professional and dedicated. They transformed our ideas into a powerful digital solution.",
    },
    {
      name: "David Wilson",
      feedback:
        "Great experience working with them. Fast, efficient, and very creative!",
    },
  ];

  return (
    <section id="testimonials" className="py-5 bg-light">
      <Container>
        <h2 className="text-center mb-4">What Our Clients Say</h2>
        <Carousel>
          {testimonials.map((t, idx) => (
            <Carousel.Item key={idx}>
              <div className="p-5 text-center">
                <p className="lead fst-italic">"{t.feedback}"</p>
                <h5 className="mt-3">- {t.name}</h5>
              </div>
            </Carousel.Item>
          ))}
        </Carousel>
      </Container>
    </section>
  );
}
